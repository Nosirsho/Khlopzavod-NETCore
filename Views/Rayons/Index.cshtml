@model IEnumerable<KlopZavod.Models.Rayon>

@{
    ViewData["Title"] = "Список";
}

<h1>Список</h1>

<p>
    <a asp-action="Create">Создать новый</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Наименование
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RayonID">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.RayonID">Подробнее</a> |
                <a asp-action="Delete" asp-route-id="@item.RayonID">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
